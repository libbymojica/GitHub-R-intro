# tips and tricks for R

#when close session, R asks if you want to save
#saves variables from environment
# tools, global options, change save setting never

# R markdown setting
# see code in line - change in tools - global options = r markdown

#use rainbow paretheses
test(x9x(x99x(((xx((())))))))

#visual editor for r markdown document
#top left on r markdown doc, "source" or "visual"

#keyboard shortcuts
#tools  keyboard shortcut help
#control + shift + m creates pipes
%>% 
  
#line up all lines
#control + shift + a
  
#create new command chunk
# control + alt + i
  
# control shift c - use when highlight code and want to assign # to
  # send to comments
  
  #button on top right of script with lines
#creates outline as heading 
# or ## 